<script lang="ts">import SortableTable, { type SortableColumn } from '#lib/components/interior/sortable-table.svelte'; const rows = [{ id: 'a', name: 'Ada', score: 4 }, { id: 'b', name: 'Bo', score: 2 }]; const columns: SortableColumn<(typeof rows)[number]>[] = [{ id: 'name', header: 'Name', value: (row) => row.name }, { id: 'score', header: 'Score', value: (row) => row.score }];</script><SortableTable label="Scores" {rows} {columns} getRowId={(row) => row.id} />
